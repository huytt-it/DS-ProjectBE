using System;
using System.Collections.Generic;
using System.Text;

namespace Data.Enum
{
    public enum LocationBrandOption
    {
        BrandName,
    }

    public enum UserOption
    {
        Name,
    }

    public enum DSBuildingOption
    {
        Name,
    }

    public enum DSMonitorOption
    {
        Name,
    }
}
